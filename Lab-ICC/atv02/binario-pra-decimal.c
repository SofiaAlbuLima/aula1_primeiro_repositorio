#include <stdio>
int main(){
    int binario;
    int decimal;
    printf("Escreva um numero binario de 4 bits para converter para decimal \n");
    scanf("%4d", &numero);

    printf("Seu numero em decimal é: %4d\n", numero);
}