// Faça um programa em C que leia 10 números inteiros e os armazene em um
// vetor. Em seguida, o programa deve:
// - Criar um segundo vetor contendo apenas os números que aparecem mais de uma vez no
// vetor original (sem repeti-los no novo vetor)
// - Exibir o vetor com os números repetidos, ou uma mensagem caso não haja repetições.

#include <stdio.h>
int main(){
    
}