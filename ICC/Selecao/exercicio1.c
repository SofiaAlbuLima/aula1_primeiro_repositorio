#include <stdio.h>
int main(){
    float a, b, c;
    scanf("%f %f %f", &a, &b, &c){
        if(a > b+c && b > c+a && c > b+a){
            printf("Os lados formarm um triângulo\n");
            if(a == b == c){
                printf("É um triângulo equilátero")
        }
    }
}
return 0;
}